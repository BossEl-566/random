{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron desktop shell for Financial Statement Studio.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "start": "electron ."
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "electron": "^43.1.1",
    "typescript": "^5.9.3"
  }
}
